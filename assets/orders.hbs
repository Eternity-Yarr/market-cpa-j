<!DOCTYPE html>
<html lang="en">
	<head>
  {{>head}}
	</head>
  <body>
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container-fluid">
        {{>navbar}}
      </div>
    </div>
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-3 col-md-2 sidebar">
          <ul class="nav nav-sidebar" id="navigation">
							<li class="text-center">Заказы</li>
						<hr />
              <li data-status="ALL"><a href="/orders">Все</a></li>
            {{#order_status}}
            	<li data-status="{{name}}"><a href="/orders?status={{name}}">{{brief}}</a></li>
            {{/order_status}}
						<hr/>
							<li><a href="/history">Последние действия</a></li>
          </ul>
        </div>
        <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
					{{>widget/pagination}}
          <div class="table-responsive">
            <table class="table table-striped table-hover">
              <thead>
                <tr>
                  <th>#</th>
                  <th>Дата</th>
                  <th>Статус</th>
                  <th>Сумма</th>
                  <th>Доставка</th>
                  <th>Оплата</th>
                  <th></th>
									<th></th>
                </tr>
              </thead>
              <tbody>
              {{#orders.orders}}
                <tr class="{{status.css_class}}">
                  <td>{{id}}</td>
                  <td>{{creationDate}}</td>
                  <td>
                      <span class="visible-md visible-lg hidden-sm hidden-xs">{{status.desc}}{{#notes}}&nbsp;<a title="{{notes}}" href="/order?id={{id}}&back_url={{back_url}}"><i class="glyphicon glyphicon-info-sign"></i></a>&nbsp;{{/notes}}</span>
                      <span class="hidden-md hidden-lg visible-sm visible-xs">{{status.brief}}{{#notes}}&nbsp;<a title="{{notes}}" href="/order?id={{id}}&back_url={{back_url}}"><i class="glyphicon glyphicon-info-sign"></i></a>&nbsp;{{/notes}}</span>
                      {{#if fake}}[ТЕСТОВЫЙ]{{/if}}
                  <td>{{currency total}}&nbsp;руб.</td>
                  <td>{{delivery.type.desc}}</td>
                  <td>
                      <span class="visible-md visible-lg hidden-sm hidden-xs">{{paymentMethod.desc}}</span>
                      <span class="hidden-md hidden-lg visible-sm visible-xs">{{paymentMethod.brief}}</span>
                  </td>
                  <td><a class="btn btn-small btn-default" href="/order?id={{id}}&back_url={{back_url}}">Подробнее</a></td>
									<td></td>
                </tr>
              {{/orders.orders}}
             </tbody>
            </table>
          </div>
					{{>widget/pagination}}
        </div>
      </div>
    </div>
  {{>scripts}}
  </body>
</html>
